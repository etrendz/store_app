<%= form_for(@stock_product) do |f| %>
  <% if @stock_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stock_product.errors.count, "error") %> prohibited this stock_product from being saved:</h2>

      <ul>
      <% @stock_product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br />
    <%= f.number_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :price %><br />
    <%= f.text_field :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
