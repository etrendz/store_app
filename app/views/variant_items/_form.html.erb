<%= form_for(@variant_item) do |f| %>
  <% if @variant_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@variant_item.errors.count, "error") %> prohibited this variant_item from being saved:</h2>

      <ul>
      <% @variant_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :variant_id %><br/>
    <%= f.collection_select :variant_id, Variant.all, :id, :name %>
  </div><br/>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
